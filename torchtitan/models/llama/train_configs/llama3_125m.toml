# torchtitan Config.toml
# NOTE: this toml config is a preset for 4–8 A100 GPUs.

[job]
dump_folder = "./outputs"
description = "Llama 3 125M training"

[profiling]
enable_profiling = true
save_traces_folder = "profile_trace"
profile_freq = 100

[metrics]
log_freq = 10
enable_tensorboard = true
save_tb_folder = "tb"

[model]
name = "llama3"
flavor = "125M"
norm_type = "rmsnorm"  # layernorm / np_layernorm / rmsnorm
tokenizer_path = "./assets/tokenizer/original/tokenizer.model"
# converters = "float8"

[optimizer]
name = "AdamW"
lr = 3e-4

[training]
batch_size = 16  # Increased since 125M is smaller
seq_len = 2048  # Standard sequence length for LLaMA-125M
warmup_steps = 2000  # Longer warmup for stability
max_norm = 1.0  # Grad norm clipping
steps = 100_000  # More steps for smaller models
data_parallel_replicate_degree = 1
data_parallel_shard_degree = -1
tensor_parallel_degree = 1
compile = false
dataset = "c4"

[experimental]
context_parallel_degree = 1
pipeline_parallel_degree = 1

[checkpoint]
enable_checkpoint = true  # Enable for smaller models
folder = "checkpoint"
interval = 50000  # Save more frequently
model_weights_only = false
export_dtype = "float32"
async_mode = "disabled" # ["disabled", "async", "async_with_pinned_mem"]

[activation_checkpoint]
mode = 'selective'  # ['none', 'selective', 'full']
selective_ac_option = 'op'  # 'int' = ac every positive int layer or 'op', ac based on ops policy

[float8]
enable_fsdp_float8_all_gather = false
precompute_float8_dynamic_scale_for_fsdp = false